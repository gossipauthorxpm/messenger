"use client"

import React from "react";
import {Stack, Typography} from "@mui/material";

export default function Page() {
    return <Stack direction={"row"} alignContent={"center"} justifyContent={"center"} width={"100vw"}>
        <Typography variant={"h5"}>SELECT CHAT</Typography>
    </Stack>
}